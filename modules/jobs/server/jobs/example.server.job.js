'use strict';

/**
 * Module dependencies.
 */
// var foo = require('bar')

module.exports = function(agenda) {
	/**
	* Module dependencies.
	*/
	/*agenda.define('say hello', function(job, done) {
		console.log("Hello world...");
	});*/
};